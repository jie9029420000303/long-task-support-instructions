# 長任務輔助指令：共用規格

此規格定義平台無關的行為；平台 adapter 只負責把角色、模型與工具映射至可用能力，不能改變本檔的不變量。

```text
任務啟動／續接 → 鎖定目標、候選版、角色配置 → 選 A／B 工作線
  A：主線設計與排版 → 子代理客觀視覺查核 ─┐
  B：子代理技術實作 → 必要時子代理真實前台操作 ─┼→ 主線同版整合查核 → 完成
                                                   └→ 未達標：判定歸屬 → 重派／第三次主線接手
```

可互動流程圖：[長任務協作流程](workflow/long-task-workflow.html)；可審閱原始規格：[workflow JSON](workflow/long-task-workflow.json)。

## 不變量

1. 主線保存原始目標、核准變更、驗收、候選版、工作包狀態與最後 PASS／FAIL。
2. A 是視覺／設計工作：主線負責設計、排版、風格判定與修正；子代理只解析來源並客觀檢查裁切、遮擋、溢出、可讀性、對齊、響應式與既定規格，不改成品或判定風格。
3. B 是系統／技術工作：子代理實作前端邏輯、後端、資料整合、修復與必要測試；主線定義驗收、查核證據與最終判定。只有同一技術包累積三次品質錯誤，主線才接手未解實作。主線可自行執行確定性的操作型步驟（執行既有指令、啟動服務、查詢資料），但任何新增／修改程式或資料轉換邏輯的工作都派技術包；主線自做的步驟必須在任務狀態登記項目與理由。
4. 網頁預設同走 A＋B；僅完全靜態、無互動、無後端的頁面只走 A。
5. 前台驗收由主線提供待驗版本、起始狀態、操作、預期結果、資料／授權邊界與證據要求；介面子代理以真實事件完成操作並回報 PASS、FAIL 或 BLOCKED。
6. 只有同一候選版的視覺、互動、API、資料、權限與錯誤狀態證據可合併為 PASS；任何改版只需重驗受影響項目與既定必要回歸。
7. 品質錯誤只包括代理交付的錯誤實作、漏步驟、缺必要證據、誤讀判準或未依指示。產品 FAIL、工具／權限／模型／來源／逾時問題與需求變更不計品質錯誤。
8. 每個工作包與工作線分開計數。第 1、2 次品質錯誤：主線具體指正，並在平台支援時提高同一代理的推理投入後重派。第 3 次：停止該包子代理鏈，由主線接手；不得無限重派。新工作包一律從該角色的基準投入起跳，不得因預估難度預先提高；覺得太難就拆包，不升檔。
9. 平台 adapter 在每個新長任務首次需要角色時，依可用且可驗證的能力鎖定主線、一般子代理與介面子代理的模型／投入；續接沿用鎖定。使用者明確指定優先，能力不支援時如實揭露，不偽裝為已套用。
10. 子代理不得自行修改正式規格、進行未授權的不可逆或敏感操作、對外發送，或替使用者做決策；未經工作包明列授權，不得安裝／移除套件、更動 lockfile 或套件管理器、重啟或重建共用服務，也不得自行選用 migration 等全域編號（由主線配發）。
11. 並行只允許可修改範圍與共用資源都互斥的工作包：同一批並行派出的工作包之間、以及與任何在途工作包之間，可修改的檔案／元件、資料表與全域編號、套件與 lockfile、共用服務（開發伺服器、資料庫、測試資料、帳號）、瀏覽器不得有交集。每個工作包必須宣告會觸碰的共用資源；主線每批派出前逐項比對並把結果記入任務狀態；有交集者改為依序（加依賴）或把共用項切給唯一一包。同一個瀏覽器工具或登入狀態同一時間只能由一個介面子代理使用；並行介面測試必須各自使用隔離的瀏覽器。只在禁止事項互相點名、範圍仍重疊，不算隔離。
12. 上下文被壓縮、續接或恢復後，主線必須先重新載入本規格的 adapter 技能與任務狀態，才可再派工或判定；自動產生的對話摘要不是規則或狀態的來源。

## 平台適配界線

- `codex/`、`claude-code/` 各自管理觸發方式、長任務機制、模型與推理控制、子代理工具、安裝位置及平台限制。
- Adapter 可加上平台特有的安全規則，不能放寬本規格。
- 如果平台不支援某項模型或子代理控制，adapter 必須採最接近且可驗證的替代方式，並揭露差距。

# Claude Code 適配任務

請先讀根目錄 `SPEC.md` 與 `tests/behavioral-cases.md`，再依 Claude Code 當前能力建立 `.claude/skills/long-task-orchestrator/SKILL.md`。

不得修改 `codex/`。不得把 Codex 專屬的 Goal、模型世代、推理值、子代理工具或安裝路徑直接複製過來。需修改共用語意時，另提變更並同時更新 `SPEC.md` 與行為案例。

完成後以實際可用的 Claude Code 長任務、子代理、模型與 Cloud 行為驗證安裝與觸發；不支援的部分要明確記錄，不可假定相容。
